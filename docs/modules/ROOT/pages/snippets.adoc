= Codex++ Linux adaptation snippets
:toc:
:toclevels: 2

English | xref:snippets_zh-CN.adoc[简体中文]

This page is both documentation and installer input.
The table is intended for maintainers; the JSON manifest is parsed by `scripts/developer/Installer-and-Manager.ps1`.

== Patch categories

Compatibility patches under `docs/modules/ROOT/attachments/patches/compatibility/` are required for the Linux port. Enhancement patches under `docs/modules/ROOT/attachments/patches/enhancements/` are optional and reserved for future user-selectable features.

== Snippet table

[cols="1,1,2,3,2,2", options="header"]
|===
|ID |Default |Target |Purpose |Injection condition |Attachment

|`010-linux-app-paths`
|Required
|`crates/codex-plus-core/src/app_paths.rs`
|Recognize Linux `start.sh` as the Codex executable, read the Linux wrapper `version` file, and recover the real app root from adapter desktop entries when stale `codex-app` state is present.
|Needed when upstream does not natively identify the `ilysenko/codex-desktop-linux` app layout.
|xref:attachment$patches/compatibility/010-linux-app-paths.patch[patch]

|`020-cdp-multi-target-injection`
|Required
|`Cargo.lock`, `crates/codex-plus-core/Cargo.toml`, `crates/codex-plus-core/src/cdp.rs`, `apps/codex-plus-launcher/src/main.rs`
|Inject all live Codex webview page targets while preferring the primary window over the hotkey window.
|Needed when Linux webview exposes several unlabeled CDP page targets or the first target is not the main window.
|xref:attachment$patches/compatibility/020-cdp-multi-target-injection.patch[patch]

|`030-linux-launcher-environment`
|Required
|`apps/codex-plus-launcher/src/main.rs`, `crates/codex-plus-core/src/launcher.rs`, `crates/codex-plus-core/src/watcher.rs`
|Launch Linux Codex Desktop through `start.sh`, preserve resolver/proxy environment, remove Electron poison variables, set safe webview ports, allow Chromium private-network helper preflights, expose helper HTTP bridge routes for settings/user scripts, keep the Linux wrapper alive after injection failure, and clear stale Linux launcher/helper processes before restart.
|Needed for the hacked Linux Codex Desktop wrapper, reliable DNS/proxy behavior in desktop sessions, Codex webview access to Codex++ helper state when CDP binding is missing or reconnecting, and recovery from stale helper ports left by crashed launcher processes.
|xref:attachment$patches/compatibility/030-linux-launcher-environment.patch[patch]

|`040-linux-entrypoints-health`
|Required
|`crates/codex-plus-core/src/install/mod.rs`, `apps/codex-plus-manager/src-tauri/src/commands.rs`, `apps/codex-plus-manager/src-tauri/src/install.rs`
|Make manager health checks detect adapter-local Linux launcher and manager binaries under `codex-app/.codex-plusplus/install`, while preserving upstream-style Codex++ desktop entry names.
|Needed when the manager reports static launch entry or management tool entry as missing on Linux.
|xref:attachment$patches/compatibility/040-linux-entrypoints-health.patch[patch]

|`050-pure-api-config-sync`
|Required
|`crates/codex-plus-core/src/relay_config.rs`, `apps/codex-plus-manager/src/App.tsx`
|Make Pure API mode write `requires_openai_auth = false`, preserve hostname BaseURL values, upgrade plain HTTP public hostnames to HTTPS, and re-apply `$HOME/.codex/config.toml` whenever the active API profile changes.
|Needed when switching API profiles in Codex++ does not update Codex Desktop immediately.
|xref:attachment$patches/compatibility/050-pure-api-config-sync.patch[patch]

|`060-renderer-linux-compatibility`
|Required
|`apps/codex-plus-manager/src/App.tsx`, `apps/codex-plus-manager/src/i18n-en.ts`, `assets/inject/renderer-inject.js`
|Stabilize Linux renderer injection for plugin marketplace requests/details, Pure API synthetic accounts, delete behavior, floating-control placement, and Fast/Standard switching. Fast supports GPT-5.4, GPT-5.5, GPT-5.6, GPT-5.6 model suffixes, localized labels, and Linux composer-footer discovery; transient backend checks do not block a known local model, and Codex++ cleanup leaves the official Fast control untouched. This patch deliberately does not restore the removed sidebar-entry unlock, ChatGPT authentication spoofing, or disabled-button mutation.
|Needed while upstream renderer injection does not account for Linux Codex Desktop DOM and MCP response differences.
|xref:attachment$patches/compatibility/060-renderer-linux-compatibility.patch[patch]

|`070-linux-user-scripts-compatibility`
|Required
|`crates/codex-plus-core/src/user_scripts.rs`
|Wrap enabled user scripts with a Linux Codex Desktop location alias so marketplace scripts that expect the upstream `app://-/index.html` renderer URL still run on the Linux localhost webview.
|Needed when user scripts such as `Codex Context Used Meter` treat the Linux `http://127.0.0.1:<port>/?mcpAppSandboxDevtools=1` page as a non-conversation window.
|xref:attachment$patches/compatibility/070-linux-user-scripts-compatibility.patch[patch]

|`080-linux-local-thread-catalog-sync`
|Required
|`crates/codex-plus-core/src/codex_sqlite.rs`, `crates/codex-plus-data/src/provider_sync.rs`
|Backfill Codex Desktop `local_thread_catalog` rows from real rollout sources while preserving non-empty titles, excluding projectless threads, and avoiding revision/sequence or backup churn when no data changed. The pinned CodexDesktop source snapshot adds companion initial-snapshot, pruning-preservation, and direct-route hydration patches.
|Needed when Codex++ can list local sessions but Linux Codex Desktop project sidebars show no conversations, when source-less remote catalog rows remain visible as empty history entries, or when shown conversations never load after click, after a Codex storage schema update.
|xref:attachment$patches/compatibility/080-linux-local-thread-catalog-sync.patch[patch]

|`900-regression-tests`
|Optional
|Nine focused test files under `apps/codex-plus-manager`, `crates/codex-plus-core/tests`, and `crates/codex-plus-data/tests`
|Add regression coverage for the nine production patches, including GPT-5.6 Fast/Standard variants, coexistence with the official Fast control, Linux user-script URL aliasing, real-title catalog backfill, idempotent catalog sync, and projectless exclusion.
|Recommended for source builds and CI; can be disabled for a faster production-only patch.
|xref:attachment$patches/compatibility/900-regression-tests.patch[patch]
|===

== Machine-readable manifest

The manager script extracts the JSON block between the markers below.

// tag::snippet-manifest[]
[source,json]
----
[
  {
    "id": "010-linux-app-paths",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/010-linux-app-paths.patch",
    "targets": [
      "crates/codex-plus-core/src/app_paths.rs"
    ],
    "purpose": "Recognize the Linux start.sh wrapper and version file, and recover from stale codex-app state through adapter desktop entries."
  },
  {
    "id": "020-cdp-multi-target-injection",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/020-cdp-multi-target-injection.patch",
    "targets": [
      "Cargo.lock",
      "apps/codex-plus-launcher/src/main.rs",
      "crates/codex-plus-core/Cargo.toml",
      "crates/codex-plus-core/src/cdp.rs"
    ],
    "purpose": "Inject every live Codex webview target and prefer the primary Codex window."
  },
  {
    "id": "030-linux-launcher-environment",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/030-linux-launcher-environment.patch",
    "targets": [
      "apps/codex-plus-launcher/src/main.rs",
      "crates/codex-plus-core/src/launcher.rs",
      "crates/codex-plus-core/src/watcher.rs"
    ],
    "purpose": "Launch Linux Codex Desktop through start.sh, preserve desktop resolver/proxy environment, expose helper HTTP bridge routes for Codex webview settings/user-script fallback, and clear stale Linux launcher/helper processes before restart."
  },
  {
    "id": "040-linux-entrypoints-health",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/040-linux-entrypoints-health.patch",
    "targets": [
      "crates/codex-plus-core/src/install/mod.rs",
      "apps/codex-plus-manager/src-tauri/src/commands.rs",
      "apps/codex-plus-manager/src-tauri/src/install.rs"
    ],
    "purpose": "Detect Linux adapter entrypoints in the Codex Desktop app-local install directory and preserve upstream-style Codex++ desktop entry names."
  },
  {
    "id": "050-pure-api-config-sync",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/050-pure-api-config-sync.patch",
    "targets": [
      "crates/codex-plus-core/src/relay_config.rs",
      "apps/codex-plus-manager/src/App.tsx"
    ],
    "purpose": "Synchronize active API profiles into $HOME/.codex/config.toml for Mixed and Pure API modes."
  },
  {
    "id": "060-renderer-linux-compatibility",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/060-renderer-linux-compatibility.patch",
    "targets": [
      "apps/codex-plus-manager/src/App.tsx",
      "apps/codex-plus-manager/src/i18n-en.ts",
      "assets/inject/renderer-inject.js"
    ],
    "purpose": "Patch Linux renderer compatibility for marketplace requests/details, Pure API account reads, delete and menu behavior, and GPT-5.6 Fast/Standard switching while preserving official Fast controls and refusing legacy plugin-entry auth spoofing."
  },
  {
    "id": "070-linux-user-scripts-compatibility",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/070-linux-user-scripts-compatibility.patch",
    "targets": [
      "crates/codex-plus-core/src/user_scripts.rs"
    ],
    "purpose": "Wrap enabled user scripts with a Linux Codex Desktop location alias for marketplace scripts that expect app://-/index.html."
  },
  {
    "id": "080-linux-local-thread-catalog-sync",
    "required": true,
    "defaultEnabled": true,
    "kind": "production",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/080-linux-local-thread-catalog-sync.patch",
    "targets": [
      "crates/codex-plus-core/src/codex_sqlite.rs",
      "crates/codex-plus-data/src/provider_sync.rs"
    ],
    "purpose": "Backfill Codex Desktop local_thread_catalog rows from rollout-backed sessions with real titles, exclude projectless threads, and keep unchanged syncs idempotent; companion CodexDesktop snapshot patches preserve and hydrate those rows."
  },
  {
    "id": "900-regression-tests",
    "required": false,
    "defaultEnabled": true,
    "kind": "test",
    "patch": "docs/modules/ROOT/attachments/patches/compatibility/900-regression-tests.patch",
    "targets": [
      "apps/codex-plus-manager/src/relay-config-contract.test.mjs",
      "crates/codex-plus-core/tests/bridge_routes.rs",
      "crates/codex-plus-core/tests/cdp_bridge.rs",
      "crates/codex-plus-core/tests/codex_sqlite.rs",
      "crates/codex-plus-core/tests/installers.rs",
      "crates/codex-plus-core/tests/launcher.rs",
      "crates/codex-plus-core/tests/relay_config.rs",
      "crates/codex-plus-core/tests/watcher.rs",
      "crates/codex-plus-data/tests/provider_sync.rs"
    ],
    "purpose": "Add regression tests for all production patches, including GPT-5.6 Fast variants, official Fast control coexistence, Linux user-script URL aliasing, real-title catalog backfill, idempotent catalog sync, and projectless exclusion."
  }
]
----
// end::snippet-manifest[]

== Patch attachments

The full patch body for each snippet is stored as an Antora attachment and included below for auditability.

[#snippet-010-linux-app-paths]
=== 010 Linux app paths

[source,diff]
----
include::attachment$patches/compatibility/010-linux-app-paths.patch[]
----

[#snippet-020-cdp-multi-target-injection]
=== 020 CDP multi-target injection

[source,diff]
----
include::attachment$patches/compatibility/020-cdp-multi-target-injection.patch[]
----

[#snippet-030-linux-launcher-environment]
=== 030 Linux launcher environment

[source,diff]
----
include::attachment$patches/compatibility/030-linux-launcher-environment.patch[]
----

[#snippet-040-linux-entrypoints-health]
=== 040 Linux entrypoints and health

[source,diff]
----
include::attachment$patches/compatibility/040-linux-entrypoints-health.patch[]
----

[#snippet-050-pure-api-config-sync]
=== 050 Pure API config synchronization

[source,diff]
----
include::attachment$patches/compatibility/050-pure-api-config-sync.patch[]
----

[#snippet-060-renderer-linux-compatibility]
=== 060 Renderer Linux compatibility

[source,diff]
----
include::attachment$patches/compatibility/060-renderer-linux-compatibility.patch[]
----

[#snippet-070-linux-user-scripts-compatibility]
=== 070 Linux user scripts compatibility

[source,diff]
----
include::attachment$patches/compatibility/070-linux-user-scripts-compatibility.patch[]
----

[#snippet-080-linux-local-thread-catalog-sync]
=== 080 Linux local thread catalog synchronization

[source,diff]
----
include::attachment$patches/compatibility/080-linux-local-thread-catalog-sync.patch[]
----

[#snippet-900-regression-tests]
=== 900 Regression tests

[source,diff]
----
include::attachment$patches/compatibility/900-regression-tests.patch[]
----

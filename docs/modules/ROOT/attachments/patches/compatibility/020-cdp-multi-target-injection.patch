diff --git a/apps/codex-plus-launcher/src/main.rs b/apps/codex-plus-launcher/src/main.rs
index cc12fae..4b7c194 100644
--- a/apps/codex-plus-launcher/src/main.rs
+++ b/apps/codex-plus-launcher/src/main.rs
@@ -517,6 +517,10 @@ impl LauncherRuntimeService {
     fn set_websocket_url(&self, websocket_url: &str) {
         *self.websocket_url.lock().unwrap() = Some(websocket_url.to_string());
     }
+
+    fn websocket_url(&self) -> Option<String> {
+        self.websocket_url.lock().unwrap().clone()
+    }
 }

 #[async_trait::async_trait]
@@ -663,10 +667,21 @@ async fn inject_with_context(
     runtime: Arc<LauncherRuntimeService>,
 ) -> anyhow::Result<()> {
     let mut last_error = None;
-    for _ in 0..20 {
+    let attempts = if cfg!(target_os = "linux") { 90 } else { 20 };
+    for attempt in 0..attempts {
         match try_inject_with_context(debug_port, helper_port, ctx.clone(), runtime.clone()).await {
             Ok(()) => return Ok(()),
             Err(error) => {
+                let _ = codex_plus_core::diagnostic_log::append_diagnostic_log(
+                    "bridge.context_inject_retry",
+                    json!({
+                        "attempt": attempt + 1,
+                        "attempts": attempts,
+                        "debug_port": debug_port,
+                        "helper_port": helper_port,
+                        "message": error.to_string()
+                    }),
+                );
                 last_error = Some(error);
                 tokio::time::sleep(std::time::Duration::from_millis(500)).await;
             }
@@ -682,12 +697,10 @@ async fn try_inject_with_context(
     runtime: Arc<LauncherRuntimeService>,
 ) -> anyhow::Result<()> {
     let targets = codex_plus_core::cdp::list_targets(debug_port).await?;
-    let target = codex_plus_core::cdp::pick_injectable_codex_page_target(&targets)?;
-    let websocket_url = target
-        .web_socket_debugger_url
-        .as_deref()
-        .ok_or_else(|| anyhow::anyhow!("selected CDP target has no websocket URL"))?;
-    runtime.set_websocket_url(websocket_url);
+    let targets = codex_plus_core::cdp::injectable_page_targets(&targets);
+    if targets.is_empty() {
+        anyhow::bail!("No injectable Codex page target found");
+    }
     let settings = codex_plus_core::settings::SettingsStore::default()
         .load()
         .unwrap_or_default();
@@ -701,18 +714,60 @@ async fn try_inject_with_context(
     } else {
         vec![script, user_bundle]
     };
-    codex_plus_core::bridge::install_bridge(
-        websocket_url,
-        codex_plus_core::bridge::BRIDGE_BINDING_NAME,
-        Arc::new(move |path, payload| {
-            let ctx = ctx.clone();
-            Box::pin(async move {
-                Ok(codex_plus_core::routes::handle_bridge_request(ctx, &path, payload).await)
-            })
-        }),
-        &new_document_scripts,
-    )
-    .await
+    let mut main_injected = false;
+    let mut injected = 0usize;
+    let mut last_error = None;
+    for target in targets {
+        let Some(websocket_url) = target.web_socket_debugger_url.as_deref() else {
+            continue;
+        };
+        let result = codex_plus_core::bridge::install_bridge(
+            websocket_url,
+            codex_plus_core::bridge::BRIDGE_BINDING_NAME,
+            Arc::new({
+                let ctx = ctx.clone();
+                move |path, payload| {
+                    let ctx = ctx.clone();
+                    Box::pin(async move {
+                        Ok(codex_plus_core::routes::handle_bridge_request(ctx, &path, payload).await)
+                    })
+                }
+            }),
+            &new_document_scripts,
+        )
+        .await;
+        match result {
+            Ok(()) => {
+                injected += 1;
+                if codex_plus_core::cdp::is_primary_codex_window(&target) {
+                    main_injected = true;
+                    runtime.set_websocket_url(websocket_url);
+                } else if runtime.websocket_url().is_none() {
+                    runtime.set_websocket_url(websocket_url);
+                }
+                let _ = codex_plus_core::diagnostic_log::append_diagnostic_log(
+                    "bridge.context_target_injected",
+                    json!({
+                        "debug_port": debug_port,
+                        "helper_port": helper_port,
+                        "target_id": &target.id,
+                        "target_url": &target.url,
+                        "primary": codex_plus_core::cdp::is_primary_codex_window(&target)
+                    }),
+                );
+            }
+            Err(error) => {
+                last_error = Some(error);
+            }
+        }
+    }
+    if main_injected {
+        return Ok(());
+    }
+    if injected > 0 {
+        anyhow::bail!("Codex++ injected only non-primary Codex page targets");
+    }
+    Err(last_error.unwrap_or_else(|| anyhow::anyhow!("Codex injection failed")))
 }

 fn default_codex_db_path() -> PathBuf {
diff --git a/crates/codex-plus-core/src/cdp.rs b/crates/codex-plus-core/src/cdp.rs
index a561c28..23614b5 100644
--- a/crates/codex-plus-core/src/cdp.rs
+++ b/crates/codex-plus-core/src/cdp.rs
@@ -58,35 +58,50 @@ async fn query_targets_url(client: &reqwest::Client, url: &str) -> anyhow::Resul
 }

 pub fn pick_page_target(targets: &[CdpTarget]) -> anyhow::Result<CdpTarget> {
-    let mut first_page = None;
-    for target in targets
-        .iter()
-        .filter(|target| is_injectable_page_target(target))
-    {
-        first_page.get_or_insert(target);
-        if is_codex_page_target(target) {
-            return Ok(target.clone());
-        }
-    }
+    injectable_page_targets(targets)
+        .first()
+        .cloned()
+        .ok_or_else(|| anyhow::anyhow!("No injectable page target found"))
+}

-    if let Some(target) = first_page {
-        return Ok(target.clone());
-    }
+pub fn pick_injectable_codex_page_target(targets: &[CdpTarget]) -> anyhow::Result<CdpTarget> {
+    injectable_page_targets(targets)
+        .into_iter()
+        .find(is_codex_page_target)
+        .ok_or_else(|| anyhow::anyhow!("No injectable Codex page target found"))
+}

-    bail!("No injectable page target found")
+pub fn pick_primary_page_target(targets: &[CdpTarget]) -> anyhow::Result<CdpTarget> {
+    injectable_page_targets(targets)
+        .into_iter()
+        .find(is_primary_codex_window)
+        .ok_or_else(|| anyhow::anyhow!("No injectable Codex page target found"))
 }

-pub fn pick_injectable_codex_page_target(targets: &[CdpTarget]) -> anyhow::Result<CdpTarget> {
+pub fn injectable_page_targets(targets: &[CdpTarget]) -> Vec<CdpTarget> {
+    let mut codex_pages = Vec::new();
+    let mut fallback_pages = Vec::new();
     for target in targets
         .iter()
         .filter(|target| is_injectable_page_target(target))
     {
         if is_codex_page_target(target) {
-            return Ok(target.clone());
+            codex_pages.push(target.clone());
+        } else {
+            fallback_pages.push(target.clone());
         }
     }
+    codex_pages.extend(fallback_pages);
+    codex_pages
+}

-    bail!("No injectable Codex page target found")
+pub fn is_primary_codex_window(target: &CdpTarget) -> bool {
+    let url = target.url.to_lowercase();
+    let title = target.title.to_lowercase();
+    !url.contains("initialroute=%2fhotkey-window")
+        && !url.contains("initialroute=/hotkey-window")
+        && !title.contains("initialroute=%2fhotkey-window")
+        && !title.contains("initialroute=/hotkey-window")
 }

 pub fn is_injectable_page_target(target: &CdpTarget) -> bool {
